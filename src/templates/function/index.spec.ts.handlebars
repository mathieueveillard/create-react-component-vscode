import {{functionName}} from ".";

describe("Test of {{functionName}}()", () => {
  test("", () => {
    // GIVEN

    // WHEN
    const actual = {{functionName}}();

    // THEN
    const expected: ReturnType = 0;
    expect(actual).toEqual(expected);
  });
});
